# This workflow is disabled for this Streamlit application
# The app is a web application, not a package requiring automated tests

name: Python package

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow disabled for Streamlit app - manual testing recommended"
